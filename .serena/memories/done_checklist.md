# Done checklist
- Before implementation, verify requirements/architecture/database documents are internally consistent.
- After implementation work exists, make sure build/test/lint commands are defined and pass; currently none exist because the repo is docs-only.
- Re-check that module boundaries, shared type locations, and AI provider abstraction rules from CLAUDE.md are preserved.
