interface:
  display_name: "Orchestrate Execute"
  short_description: "Phase 2: compile plan and run execution."
  default_prompt: "Use $orchestrate-execute to compile the execution plan from GitHub task issues, run doctor/preflight, and execute the orchestrator safely."
policy:
  allow_implicit_invocation: false
